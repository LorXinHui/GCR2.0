<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">

        <activity android:name=".ActivityRegister"
            android:exported="true">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".ActivityLogin"/>
        <activity android:name=".activity.FragmentMesssage" />
        <activity android:name=".activity.FragmentNews" />
        <activity android:name=".activity.FragmentNewsDetails" />
        <activity android:name=".activity.FragmentCasual" />
        <activity android:name=".activity.FragmentQuiz" />
        <activity android:name=".activity.FragmentProfessional"/>
        <activity android:name=".activity.FragmentCourseDetails"/>
        <activity android:name=".activity.FragmentSettings" />
    </application>

</manifest>